{% extends 'base.html.twig' %}

{% block title %}Galerie d'images d'accueil {% endblock %}
{% block content %}
<div class="row alert alert-warning" role="alert">
<div class="col-xs-9 col-sm-10"></div>
<div class="col-xs-3 col-sm-2">
    <a href="{{ path('app_image_accueil_new') }}"role="button" class="btn btn-info text-right"style="background-color: #b18914; color: #ffffff; border-color:  #b18914;">
        <i class="fa fa-plus fa-fw"></i>
        💡Changer l'image d'accueil            
    </a>
</div>
</div>
<div class="container">
    <h1>Galerie d'images d'accueil</h1>

    <table class="table">
        <thead>
            <tr>
                <th>N°</th>
                <th>Nom de l'image</th>
                <th>Fichier d'image</th>
                <th>Date de mise à jour</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% set z = 1 %}
        {% for image_accueil in image_accueils %}
            <tr>
                <th scope="row">
                {{z}}
                </th>
                <td>{{ image_accueil.nom }}</td>
                <td><img src="{{ vich_uploader_asset(image_accueil, 'imageFile') }}"  width="280" height="380" alt="{{ image_accueil.nom }}"></td>
                <td>{{ image_accueil.updatedAt ? image_accueil.updatedAt|date('Y-m-d') : '' }}</td>
                {# <td>{{ image_accueil.createdAt ? image_accueil.createdAt|date('Y-m-d H:i:s') : '' }}</td> #}
                <td>
                <a href="{{ path('app_image_accueil_show', {'id': image_accueil.id}) }}"role="button" class="btn btn-info text-right"style="background-color: #b18914; color: #ffffff; border-color:  #b18914; width: 100px;">Visualiser</a><br>
                <br> <a href="{{ path('app_image_accueil_edit', {'id': image_accueil.id}) }}"role="button" class="btn btn-info text-right"style="background-color: #b18914; color: #ffffff; border-color:  #b18914; width: 100px;"> Modifier </a>
                </td>
                {% set z = z + 1 %}
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    
</div>
</div>
    {# <a href="{{ path('app_image_accueil_new') }}">Create new</a> #}
{% endblock %}
{% block stylesheets %}


<style>
.table{
    background-color:#f5f5f5;
    }
h2{
    font-family: Bodoni, serif;
    letter-spacing: .05em;
    font-size: 2em;
    margin-bottom: 1em;
    font-style: italic;
    color: #cd9700;
    font-weight: 400;
    text-align: center;
}
.container{
    max-width: 100%;
    border: 1px solid #ddd;
    border-radius: 10px;
    background-color: #F9F9FA;
    color: #413C49;
    margin-top:10px;
    margin-bottom: 20px;
    padding-bottom: 5px;

} 
.alert{
    margin-top:10px;
}

</style>
{% endblock %}