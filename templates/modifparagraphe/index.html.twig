 {% extends 'base.html.twig' %}

{% block title %}Les paragraphes{% endblock %}

{% block content  %}
<div class="row alert alert-warning" role="alert">
    <div class="col-xs-9 col-sm-10"></div>
    <div class="col-xs-3 col-sm-2">
        <a href="{{ path('app_modifparagraphe_new') }}"role="button" class="btn btn-info text-right"style="background-color: #b18914; color: #ffffff; border-color:  #b18914;">
            <i class="fa fa-plus fa-fw"></i>
            💡Changer la paragraphe            
        </a>
    </div>
</div>
<div class="container">
    <h2>Les paragraphes</h2>

    <table class="table">
        <thead>
            <tr>
                <th>N°</th>
                <th style="width: 70%">Les paragraphes</th>
                <th style="width: 15%">Date de mise à jour</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% set z = 1 %}
        {% for modifparagraphe in modifparagraphes %}
            <tr>
                <th scope="row">{{z}}</th>
                <td>{{ modifparagraphe.paragraphe }}</td>
                <td>{{ modifparagraphe.updatedAt ? modifparagraphe.updatedAt|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_modifparagraphe_show', {'id': modifparagraphe.id}) }}"role="button" class="btn btn-info text-right"style="background-color: #b18914; color: #ffffff; border-color:  #b18914; width: 100px;">Visualiser</a><br>
                    <br> <a href="{{ path('app_modifparagraphe_edit', {'id': modifparagraphe.id}) }}"role="button" class="btn btn-info text-right"style="background-color: #b18914; color: #ffffff; border-color:  #b18914; width: 100px;"> Modifier </a>
                </td>
                {% set z = z + 1 %}
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
{% block stylesheets %}
<style>
.table{
    background-color:#f5f5f5;
}
h2{
    font-family: Bodoni, serif;
    letter-spacing: .05em;
    font-size: 2em;
    margin-bottom: 1em;
    font-style: italic;
    color: #cd9700;
    font-weight: 400;
    text-align: center;
}
.contai
.alert{
    margin-top:10px;
}
th{
    white-space: nowrap;
}
</style>
{% endblock %}
