{% extends 'base.html.twig' %}

{% block title %}SeuilMaximum{% endblock %}

{% block content %}
    <h1>SeuilMaximum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seuil_maximum.id }}</td>
            </tr>
            <tr>
                <th>SeuilMaximum</th>
                <td>{{ seuil_maximum.seuilMaximum }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seuil_maximum_index') }}">back to list</a>

    <a href="{{ path('app_seuil_maximum_edit', {'id': seuil_maximum.id}) }}">edit</a>

    {{ include('seuil_maximum/_delete_form.html.twig') }}
{% endblock %}
