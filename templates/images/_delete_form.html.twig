<form method="post" action="{{ path('app_images_delete', {'id': image.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image.id) }}">

<button class="btn btn-danger btn-lg btn-block" style="width:400px;display: block;
margin: 0 auto;">Supprimer</button>
</form>
